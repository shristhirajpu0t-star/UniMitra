<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Uniमित्र</title>

  <!-- PDF + DOCX extract libraries -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.12.313/pdf.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/mammoth/1.4.17/mammoth.browser.min.js"></script>

  <script>
    pdfjsLib.GlobalWorkerOptions.workerSrc =
      "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.12.313/pdf.worker.min.js";
  </script>

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- External CSS file -->
  <link rel="stylesheet" href="style.css" />
</head>

<body class="bg-gray-50 min-h-screen">

  <!-- Login Container -->
  <div id="login-container" class="flex items-center justify-center min-h-screen bg-gray-50">
    <div class="bg-white p-8 rounded-xl shadow-md w-[350px]">
      <h2 class="text-2xl font-bold mb-6 text-center">Login</h2>
      <form id="login-form" class="flex flex-col gap-4">
        <input type="text" id="name" placeholder="Name" required class="p-3 border rounded-lg">
        <input type="email" id="email" placeholder="Email ID" required class="p-3 border rounded-lg">
        <button type="submit" class="bg-blue-600 text-white py-2 rounded-lg">Login</button>
      </form>
    </div>
  </div>

  <!-- Main App Container -->
  <div id="app-container" class="hidden">
    <header class="gradient-bg text-white px-6 py-4 flex justify-between items-center">
      <div>
        <h1 class="text-3xl font-bold">UniMitra</h1>
        <p class="text-sm opacity-90 mt-1" id="user-name-display">Your Study Companion</p>
      </div>
      <button id="logout-btn" class="bg-red-500 hover:bg-red-600 px-4 py-2 rounded">Logout</button>
    </header>

    <div id="app"></div>
    <div id="modal-container"></div>
  </div>

  <!-- JS -->
  <script src="script.js"></script>

  <script>
    const loginContainer = document.getElementById('login-container');
    const appContainer = document.getElementById('app-container');
    const userNameDisplay = document.getElementById('user-name-display');

    const userName = sessionStorage.getItem('userName');
    const userEmail = sessionStorage.getItem('userEmail');

    // If user already logged in
    if (userName && userEmail) {
      loginContainer.classList.add('hidden');
      appContainer.classList.remove('hidden');
      userNameDisplay.textContent = Hello, ${userName};
      if (typeof initialize === "function") initialize();
    }

    // Login form
    document.getElementById('login-form').addEventListener('submit', (e) => {
      e.preventDefault();
      const name = document.getElementById('name').value.trim();
      const email = document.getElementById('email').value.trim();
      if (!name || !email) return alert('Please enter both Name and Email ID.');
      sessionStorage.setItem('userName', name);
      sessionStorage.setItem('userEmail', email);
      loginContainer.classList.add('hidden');
      appContainer.classList.remove('hidden');
      userNameDisplay.textContent = Hello, ${name};
      if (typeof initialize === "function") initialize();
    });

    // Logout
    document.getElementById('logout-btn').addEventListener('click', () => {
      sessionStorage.removeItem('userName');
      sessionStorage.removeItem('userEmail');
      appContainer.classList.add('hidden');
      loginContainer.classList.remove('hidden');
    });
  </script>

</body>
</html>